entryPoints:
  iotnetwork:
    address: ':80'
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ':443'
api:
  dashboard: true
  insecure: true
certificatesResolvers:
  lets-encrypt:
    acme:
      email: marcomedinaG@proton.me
      storage: /acme.json
      tlsChallenge: {}
providers:
  docker:
    watch: true
    network: iotnetwork
  file:
    filename: traefik_api.yml
